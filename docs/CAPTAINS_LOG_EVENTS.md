# Captain's Log — Event Contract

*The single reference for every captain's-log event: what it is, who emits it, what fields it carries, and when it fires. A blind collaborator should be able to reason about observability from this doc alone, without reading every emitter.*

Source of truth for the event-type constants: `src/captains_log.py` (the `EVENT_TYPES` set). Source of truth for emission: the call sites listed below. This doc is generated by inventory + hand-annotation (2026-06-24); re-run the sweep (grep `log_event(` across `src/`) if it drifts.

---

## What the captain's log is

An append-only, human-readable changelog of what the **learning system** decides about its own knowledge. Not raw data (that's `outcomes.jsonl`), not aggregated metrics (that's the dashboard) — a narrated record of skill/lesson/rule/scope/loop lifecycle transitions.

- **File:** `~/.poe/workspace/memory/captains_log.jsonl` (one JSON object per line).
- **Rotation:** size-gated, riding on `log_event` (no cron — the no-scheduler invariant). When the active file exceeds `captains_log.rotate_mb` (default 5), all but the most recent `captains_log.rotate_keep` (default 1000) entries move to a timestamped archive `captains_log.<stamp>.jsonl`. Data is never deleted. The rotation itself emits `LOG_ROTATED`.
- **Readers:** the hot-path reader (`load_log`) reads the active file only; archaeology readers (`query_log`, `timeline`) span archives.

## Entry schema

Written by `captains_log.log_event(...)`. Every entry has the four required fields; the rest are present only when supplied.

| Field | Required | Type | Meaning |
|-------|----------|------|---------|
| `timestamp` | yes | ISO-8601 UTC | When the event was appended. |
| `event_type` | yes | string | One of the constants below. |
| `subject` | yes | string | The thing the event is about (skill id, rule id, goal preview, loop id, …). |
| `summary` | yes | string | One-line human-readable description. |
| `context` | no | object | Event-specific structured fields (the per-event tables below). |
| `note` | no | string | Optional caveat / interpretation. |
| `loop_id` | no | string | Owning loop, when the event happens inside a run. |
| `related_ids` | no | list[string] | Cross-references (e.g. `["skill:jina-x-scraper"]`). |

`log_event` never raises on I/O failure (it logs a warning) — emission must never break the pipeline.

---

## Events by category

`context` columns list the **field names** passed at the call site. "(dynamic)" means the context dict is built from a variable, so the exact keys vary by code path.

### Skill lifecycle

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `SKILL_SYNTHESIZED` | evolver.py:2620 | skill_id, goal, outcome | A new skill was synthesized from a successful outcome. |
| `SKILL_SYNTHESIS_REJECTED` | evolver.py:2545 | gate, reason, goal, trigger_patterns | The 3-gate quality check rejected a skill candidate. |
| `SKILL_PROMOTED` | skills.py:1203 | skill_id, utility, use_count | A provisional skill crossed the promotion threshold (5+ uses / 70%+ success). |
| `SKILL_DEMOTED` | skills.py:1264 | skill_id, utility, circuit_state | A skill fell below the utility floor and was demoted. |
| `SKILL_REWRITE` | *(none — see gaps)* | — | Intended: a skill definition was rewritten. Not currently emitted. |
| `SKILL_CIRCUIT_OPEN` | skills.py:1001 | skill_id, utility_before, utility_after, consecutive_failures, consecutive_successes | A skill hit the consecutive-failure threshold; circuit breaker opened (skill disabled). |
| `SKILL_CIRCUIT_HALF_OPEN` | skills.py:1001 (dynamic state mapping) | *(same as above)* | Cooldown elapsed; skill gets a trial invocation. |
| `SKILL_CIRCUIT_CLOSED` | skills.py:1001 (dynamic state mapping) | *(same as above)* | Trial succeeded; circuit closed (skill re-enabled). |
| `SKILL_VARIANT_CREATED` | skills.py:1479 | parent_id, challenger_id | An A/B challenger variant of a skill was created. |
| `AB_RETIRED` | skills.py:1617 | promoted, retired | An A/B test concluded; the winner was promoted and the loser retired. |
| `ISLAND_CULLED` | skills.py:557 | culled_ids | Orphaned/low-value skill "islands" were culled. |

### Knowledge crystallization (lessons, hypotheses, rules)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `LESSON_RECORDED` | knowledge_web.py:267 | tier, task_type, confidence, lesson_type | A lesson was extracted from an outcome and stored in tiered memory. |
| `LESSON_REINFORCED` | knowledge_web.py:454 | tier, sessions_validated, score, promoted | An existing lesson was re-confirmed (and possibly promoted MEDIUM→LONG). |
| `LESSON_DECAYED` | gc_memory.py:192 | tier, removed, remaining | Decay/GC removed lessons that fell below the retention score. |
| `LESSON_RECOVERED` | *(none — see gaps)* | — | Intended: a decayed lesson was recovered. Not currently emitted. |
| `HYPOTHESIS_CREATED` | knowledge_lens.py:270 | domain, hyp_id | A new hypothesis was formed from observed patterns. |
| `HYPOTHESIS_PROMOTED` | knowledge_lens.py:336 | domain, confirmations, rule_id | A hypothesis accrued enough confirmations to become a standing rule. |
| `HYPOTHESIS_CONTRADICTED` | knowledge_lens.py:297 (hyp_id, conflicting_rule_id); knowledge_lens.py:393 (contradictions, confirmations) | *(varies by call site)* | A hypothesis was contradicted by a rule or by new evidence. |
| `STANDING_RULE_CONTRADICTED` | knowledge_lens.py:368 | contradictions, confirmations | A promoted standing rule was contradicted by recent experience. |
| `RULE_REFOUGHT` | knowledge_lens.py:673 | action (keep\|revise\|retire), reasoning, old_rule, new_rule, contradictions, confirmations | A contested standing rule was re-derived against its contradiction evidence (decay-by-invalidation). The audit trail for trust changes. |
| `RULE_VERIFIED` | knowledge_lens.py:229 | confirmations, contradictions, last_verified | A promoted rule was re-confirmed in production; stamps `last_verified` (freshness signal that keeps it out of the stale verify-before-relying block). |
| `RULE_GRADUATED` | rules.py:273 | rule_id, skill_id, pass3 | A repeated failure-class diagnosis graduated to a permanent rule. |
| `RULE_DEMOTED` | rules.py:325 | rule_id, wrong_answers | A standing rule produced wrong answers and was demoted. |
| `CANON_CANDIDATE` | *(none — see gaps)* | — | Intended: a lesson/rule became a canon (identity) candidate. Not currently emitted. |
| `MEMORY_CONSOLIDATED` | knowledge_web.py:713 | (dynamic: cycle) | In-process consolidation (decay-consequence promote/GC) rode along an app-lifecycle call. |
| `LOG_ROTATED` | captains_log.py:344 | archived, retained, archive | The active log exceeded its size gate; older entries moved to a timestamped archive. First entry of each fresh active file. |

### Evolver (meta-improvement)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `EVOLVER_GENERATED` | evolver.py:1889 | run_id, outcomes_reviewed, suggestions, auto_applied, patterns | An evolver cycle ran and produced improvement suggestions. |
| `EVOLVER_APPLIED` | evolver.py:353 | suggestion_id, category, confidence | A low-risk evolver suggestion was auto-applied. |
| `EVOLVER_SKIPPED` | evolver.py:1902 | run_id, outcomes_reviewed | An evolver cycle ran but produced nothing to apply. |
| `EVOLVER_REVERTED` | evolver.py:663 *(unregistered — see gaps)* | suggestion_id, category, target | An applied suggestion was rolled back. |
| `EVOLVER_VERIFY` | evolver.py:2071 *(unregistered — see gaps)* | run_id, auto_applied, passed, summary, reverted | Post-apply verification (pytest) ran on auto-applied changes. |
| `GRADUATION_PROPOSED` | graduation.py:353 | category, confidence | A repeated failure-class diagnosis was proposed for graduation to a permanent fix. |

### Recovery & diagnosis

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `AUTO_RECOVERY` | agent_loop.py:5078 | action, risk, params | The loop's auto-recovery path fired after a stuck/failed step. |
| `DIAGNOSIS` | introspect.py:466 | severity, steps_done, steps_blocked, tokens | A run-level diagnosis was produced (failure classification). |
| `INPUT_MISMATCH` | skills.py:1033 | skill_id, input_type, skill_url_domain, consecutive_failures | A skill was invoked on out-of-domain input (e.g. a web-content skill given code). |

### Decisions

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `DECISION_RECORDED` | knowledge_lens.py:779 | domain, alternatives | A decision (with considered alternatives) was recorded. |
| `METACOGNITIVE_DECISION` | agent_loop.py:606 (step_idx, retries, fingerprints, replan_count, action); agent_loop.py:3961 (action, bump_amount, new_max_iterations, completion_rate, steps_remaining) | *(varies by call site)* | A mid-loop re-decompose/retry/budget-bump decision was made. |

### Scope + closure observability (Phase 65)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `SCOPE_GENERATED` | handle.py:1504 | (dynamic: scope ctx) | Scope generation produced a scope for a goal. |
| `SCOPE_PARSE_FAILED` | handle.py:1434 | goal_preview, raw_length, raw_preview | Scope generation returned output that failed to parse. |
| `SCOPE_SKIPPED` | handle.py:1411 (goal_preview, reason); handle.py:1543 (goal_preview, reason, error) | *(varies by call site)* | Scoping was enabled but produced nothing (adapter error/None). Without this, the May-2026 rc=1 outage silently lost every run's scope. |
| `CLOSURE_VERDICT` | director.py:1708 | goal_preview, complete, confidence, checks_run, checks_passed, gap_count, scope_supplied, modality_distribution, inconclusive_count, behavioral_gap_downgrade, diagnosis_failure_class, diagnosis_gap_downgrade, commands, summary | Closure completed its verification pass; the full per-check distribution + verdict. |
| `CLAIM_PROBED` | quality_gate.py:565 | claim_preview, reviewer_verdict, final_verdict, probe_command, probe_status, probe_exit_code, probe_output_preview | Adversarial-review grounding: an individual claim was probed and reconciled. |
| `CLAIM_VERIFIER_OUTCOME` | agent_loop.py:1211 | step_idx, outcome, action, file_not_found, file_verified_count, symbol_not_found, symbol_verified_count | Per-step: file/symbol claims in a step result were verified and a downstream action taken. |

### Run transparency (loop lifecycle + quality gate)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `LOOP_CREATED` | agent_loop.py:2398 | reason, parent_loop_id, project, max_steps, continuation_depth, dry_run | Every loop spawn (root or continuation). |
| `QUALITY_GATE_VERDICT` | quality_gate.py:364 | decision, verdict, confidence, confidence_threshold, escalate, reason, step_count | The post-loop quality gate decided PASS or ESCALATE — the most important escalation signal. |
| `STEP_TOO_BROAD` | agent_loop.py:989 | step_index, elapsed_s, tokens, cap_elapsed_s, cap_tokens, project | A step exceeded the resource cap (≤120s / ≤200K tokens). Fires mid-loop so the warning is actionable before the loop finishes. |

### Recall + dispatch guard (goal-brain)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `RECALL_PERFORMED` | recall.py:425 | goal_preview | Every `recall()` call (crystallization substrate). |
| `RECALL_GUARD_TRIPPED` | handle.py:2298 | goal_preview, job_id | The dispatch guard refused to re-run a goal whose recent attempts all failed (the ~25× repeat-burn protection). |

### Navigator (goal-brain)

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `NAVIGATOR_DECIDED` | navigator_prompt.py:214 | (dynamic: navigator ctx) | One per navigator invocation (shadow or live): input digest, decision, tier, and (shadow) what the static pipeline did. |
| `NAVIGATOR_ACTED` | handle.py:2149 | point, move, confidence, reasoning, goal_preview, job_id, source, status | One per navigator decision that actually changed dispatch behavior (per-class cutover made real). |

### Validation shadow-eval

| Event | Emitter(s) | `context` fields | When it fires |
|-------|-----------|------------------|---------------|
| `VALIDATOR_SHADOWED` | validation_shadow.py:85 | step_class, step_preview, local_passed, local_confidence, local_source, paid_passed, paid_confidence, paid_source, agreement, escalated | One per validated step while `validate.shadow_eval` is on: local (free) vs paid verdict on the same result. Decide-only; nothing reads it for control flow. |

---

## Integrity gaps (found during the 2026-06-24 inventory)

These are not bugs in running code, but contract drift worth tracking. They live in BACKLOG (#8 follow-up).

### Defined in `EVENT_TYPES` but never emitted (3)

`CANON_CANDIDATE`, `LESSON_RECOVERED`, `SKILL_REWRITE`. These constants exist but no production code emits them. Either the feature was never wired (canon promotion / lesson recovery are known Stage 2→3 gaps per `docs/ARCHITECTURE_OVERVIEW.md` §3) or the emitter was removed. Don't assume these appear in real logs.

Note `SKILL_REWRITE` is worse than merely unemitted: it is **referenced by consumers** (`recall.py:54` and `evolver.py:995` both list it in their event-of-interest sets) while nothing ever produces it — a dead expectation. The skill-rewrite path either never logs, or logs under a different event.

### Emitted but NOT registered in `EVENT_TYPES` (3)

`EVOLVER_REVERTED` (evolver.py:663), `EVOLVER_VERIFY` (evolver.py:2071), `PLAYBOOK_UPDATED` (playbook.py:234). These fire in production with string literals that aren't in the `EVENT_TYPES` set. Consumers that validate against `EVENT_TYPES` (e.g. a strict renderer or filter) will silently drop them. Fix: add the three constants to `captains_log.py` and the `EVENT_TYPES` set, and switch the emitters to the constants.

### Multi-signature events

A few events are emitted from more than one call site with different `context` shapes — consumers must treat `context` keys as optional per event:
- `HYPOTHESIS_CONTRADICTED` — two signatures (hyp-vs-rule conflict; evidence re-tally).
- `METACOGNITIVE_DECISION` — two signatures (mid-loop retry/replan; budget bump).
- `SCOPE_SKIPPED` — two signatures (skipped-with-reason; skipped-on-error).

---

*45 distinct event types actively emitted across ~52 call sites in 16 modules. Last inventory: 2026-06-24.*
