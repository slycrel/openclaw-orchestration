# OpenClaw workspace root (optional; auto-detected when unset)
OPENCLAW_WORKSPACE=

# Optional queue backend selector used by wrapper scripts
QUEUE_BACKEND=local

# Optional runtime verbosity
ORCH_LOG_LEVEL=info
